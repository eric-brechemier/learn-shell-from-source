PATH="$(dirname $0):$(dirname $(command -v dirname))"

# A PIPE SEQUENCE IS:

# a command
command

# OR a sequence of commands separated by control operator '|'
command | command | command

# with OPTIONAL line breaks
command |
command |
command
